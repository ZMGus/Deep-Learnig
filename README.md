# Detección de Transacciones Fraudulentas con MLP y Random Forest

Este proyecto implementa modelos de aprendizaje automático (MLP y Random Forest) para detectar transacciones fraudulentas en tarjetas de crédito. Utiliza un conjunto de datos altamente desbalanceado y emplea técnicas de visualización e interpretabilidad como SHAP y UMAP.

## 🔧 Requisitos

Antes de ejecutar el proyecto, asegúrate de tener Python instalado. Luego instala las librerías necesarias:

```bash
pip install -r requirements.txt
````

## 🚀 Cómo correr el proyecto

El proyecto está contenido en 1 archivo Jupyter Notebook. Para correrl:

1. Clona el repositorio o descarga el archivo localmente.
2. Abre el notebook en JupyterLab, Jupyter Notebook o VS Code.
3. Asegúrate de que tu entorno virtual tenga las librerías instaladas.
4. Ejecuta todas las celdas en orden. No se requiere ninguna configuración adicional.

## 📁 Estructura esperada del proyecto

```
├── notebook.ipynb
├── requirements.txt
└── README.md
```

## 📌 Notas

* El conjunto de datos se espera ya cargado en tu entorno local (No se puede subir debido al tamaño).
* No se necesita conexión externa o autenticación.
* Compatible con entornos locales en CPU (no se requiere GPU).
```

